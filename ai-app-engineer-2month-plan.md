# RAG 落地实践计划（6 周主线 + 第 7–12 周按场景加深）

> **真正目的**：具备 RAG 落地经验——搞清**全流程**；坑点来自你**做的过程中真实撞上的问题**，不是事先背一张坑点清单。  
> 可以用 AI 写大部分代码；你必须自己做决策、看 trace、跑 eval、解释「为什么这样改」。  
> 主线产品：**领域知识助手**（混合语料 RAG）。  
> 节奏：每周约 15–20h · 通用工程已具备 · 已练过 8 个 agent demo。  
> 完成标准：能独立讲清一条完整 RAG 链路；`rag-pitfalls.md` 里记的都是你亲手遇到、排查、验证过的事。  
> **执行纪律**：**第 1–6 周按原主线继续往下做，不提前跳后期**；第 7–12 周仅在主线复盘完成、且你自己觉得「原先计划已掌握」之后再开。  
> **节奏约定（与前三周一致）**：每周一个清晰主题、直接上正确方案并讲清 why、用手测/trace/eval 验收；**循序渐进，不并行开多条后期线**。  
> **文档导航**：迷路先看 [`README.md`](README.md) 顶部 3 文档表；demo vs 生产看 [`docs/production-gap.md`](docs/production-gap.md)。  
> **后期纪律**：按「场景 → 补语料（独立知识库）→ 该库专属策略/Profile → 注册为 Agent 工具 → 对照」推进；**不为「现在没有该语料」而跳过策略**。  
> **后期能力主轴（主线完成后再练）**：不同语料 = 不同知识库 = 不同处理策略 = 不同工具；智能体选工具做 RAG 路由。

---

## 0. 原计划够不够？结论

| 你的需求 | 原计划覆盖情况 | 结论 |
|---------|---------------|------|
| 跑通 RAG 全流程 | 阶段一～二很强 | ✅ 满足 |
| 过程中真实遇到的问题 | 有流程，但容易变成「预习坑点」 | ⚠️ 需改方法 |
| 落地经验（非刷课） | 「能力长在产品上」正确 | ✅ 满足 |
| 允许 AI 辅助开发 | 原计划偏「亲手工程到求职」 | ⚠️ 需改定位 |
| Agent / 多库路由 / 多场景策略（含图谱·多模态·纠错型 RAG） | 原 1–6 周覆盖不足 | ✅ **收到第 7–12 周：按场景补语料再练** |

**总评**：原计划 **第 1–6 周继续作为唯一主线**，保持前三周那种循序渐进节奏；第 7–12 周是「主线掌握之后」的加深，用来覆盖分库工具路由与多场景策略——**现在不并行、不提前。**

**原则**：
1. **先主线、后加深**：没完成第 6 周 DoD，不开第 7 周及以后
2. 每周主题单一，与第 1–3 周相同：方案讲清 why → 实现 → 手测/eval → 记日志
3. 坑点只事后记录；AI 可写代码，决策与验证自己做
4. 每加一招要有 before/after
5. 后期场景周才「补语料 → 分 KB → Profile → Tool → Agent 路由」

---

## 坑点怎么来（重要）

**做法（效率优先）**：
- **直接上正确方案**，并说明：为什么用它、好处是什么、解决什么问题
- 不故意先用错误方案制造问题，再「揭晓」正确做法
- 过程中真实撞上的问题仍记入 `rag-pitfalls.md`（事后实录，不是预习作业）

**不做的事**：
- 不预先列出「本周典型坑」让你对照打卡
- 不故意制造假故障 / 不先埋雷再挖雷
- 不把别人的坑点文章抄进手册冒充经验

**做的事**：
- 按周推进真实能力（ingest → 检索 → eval → 产品形态）
- 用**真实语料 + 自己出的题 + trace/eval** 工作——问题会自己冒出来
- 一旦出现：答错、检索怪、解析炸、分数掉、改完变差……**当场记入** `rag-pitfalls.md`
- 记的时候写清：当时在做什么、怎么发现的、怎么定位的、改没改、改完怎样

**计划的作用**：让你完整走过链路，并具备「看见问题」的手段（手测、trace、eval）。  
**手册的内容**：100% 来自你当时的现场，写不出就说明还没撞上——继续做，别编。

若某周流程走完、手里暂时没有新坑：正常，写「本周未新增；卡点在某某能力还没暴露问题」即可，**不要为了凑条数去找坑**。

---

## 1. 你要掌握的 RAG 全流程（心智模型）

落地时脑子里应始终是这条链路，而不是一堆框架名：

```
语料获取 → 解析清洗 → 分块(chunk) → 元数据 → Embedding → 入库/建索引
                                                      ↓
用户问题 → (可选) Query 改写 → 检索(向量/关键词/混合) → 重排 → 拼 Prompt
                                                      ↓
                                              LLM 生成 → 引用来源 → 评估/观测
```

**运维侧两刀**（做到那一步时自然会碰到要不要处理）：
- 语料更新后如何重建/增量索引
- 改了分块或模型后如何用 eval 看有没有退步

---

## 2. AI 辅助开发的边界

| 可以交给 AI | 必须自己做 / 自己过一遍 |
|------------|------------------------|
| 样板代码、配置、胶水代码 | 选定分块策略并说出 why |
| 写 parser / 调用 SDK | 跟一条失败/奇怪请求的 trace |
| 写 eval 脚本骨架 | 手写/审核 golden set |
| 修语法错误、补类型 | 对比改动前后的分数或案例 |
| README 初稿 | 用自己的话讲流程 + 手册里真实条目 |

**红线**：只有「AI 生成了能跑的代码」，却说不清本周动了链路哪一环、依据是什么 → 本周不算完成。

---

## 3. 总体路线图（6 周主线 → 后期按场景补语料练策略）

```
【主线 · 继续执行，不跳周】
第1周              第2周               第3周★             第4周★
最简 RAG baseline   可观测 + 可靠性      质量工程(分块/检索)   Eval 体系 + 回归
解析→切→嵌→检索→答   Langfuse + 降级      混合检索/重排         一键对比 before/after
手测时记下异常       最小 golden set       （改写留给第5周）      recall@k + 扩题

第5周              第6周
端到端产品形态       复盘固化
引用/拒答/多轮改写   pitfalls + 架构图 + demo
最小界面

【后期 · 主线完成后再开｜语料分库 → Profile 分策略 → 工具化 → Agent 路由】
第7周★             第8周★                    第9周★
单库能力补齐         多语料→多 KB + Profile     Agent RAG 路由骨架
过滤/子查询/窗口     每库独立策略；补 PDF        LLM 选 search_* 工具

第10周★            第11周★                   第12周★
生产存储改造         Graph RAG（Neo4j）       多模态 + CRAG + 复盘
Qdrant/OpenSearch   query_relations           search_visual
```

**当前进度（2026-08-16）**：第 1–**10** 周已完成并验收；**当前 = 第 11 周 Graph RAG**。  
**第 10 周执行文档**：[`docs/production-upgrade.md`](docs/production-upgrade.md)（只看 Phase 进度表）。  
**Demo vs 生产**：[`docs/production-gap.md`](docs/production-gap.md)。  
**跑通与命令**：[`README.md`](README.md)。

**后期总原则（2026-07-28 订正）**：
- **能力目标**：掌握「分库 RAG + Agent 工具路由」——不是只调单个 pipeline。
- **设计约定**（全程按此实现）：
  1. **一种语料类型 / 一个领域包 → 一个知识库（KB）**
  2. **一个 KB → 一套 Profile**（解析、分块、检索、重排、过滤、prompt 等可与公共默认不同）
  3. **一个 KB → 一个（或一组）Agent 工具**（如 `search_hr_policy`、`search_pdf_handbook`、`query_relations`、`search_visual`）
  4. **智能体只负责选工具 / 传 query**；库内优化策略封装在工具背后，不靠模型「临场发明」
- 目标是覆盖 **不同语料场景 ↔ 对应优化策略**；**缺语料就到当周补成独立 KB 包**，再挂 Profile 与工具。
- 每场景要求：专项 golden + before/after；新 KB 上线时同时验收 **路由是否选对工具**。

**贯穿全程**：
- [`README.md`](README.md)：跑通、命令、**文档导航（只看 3 个）**
- [`docs/production-gap.md`](docs/production-gap.md)：demo 简化 vs 生产默认 vs 升级触发
- [`rag-pitfalls.md`](rag-pitfalls.md)：**只追加真实遇到的问题**（无则不写）

---

## 4. 主线产品

**产品**：**公司内部知识助手**（虚构「星云科技」）——回答请假报销、IT 权限、安全分级、产品对内口径等员工问题；带来源，便于判断错在哪一环。

**语料风格**：国内常见的内部文档（中文为主、夹杂系统名/英文缩写），不是开源框架英文文档。

| 类型 | 本仓库位置 | 说明 |
|------|-----------|------|
| Markdown 制度/FAQ/SOP | `data/corpus/internal/markdown/` | 已接入 |
| HTML 内部 Wiki | `data/corpus/internal/html/` | 已接入 |
| CSV 通讯录等 | `data/corpus/internal/csv/` | 已接入 |
| PDF 制度 / 长文档 | `data/corpus/internal/pdf/` | **第 8 周补语料并练**解析与分块（缺则当场补，不跳过） |
| 关系型（组织/依赖/审批链） | prose 制度 + 架构 MD + CSV | **第 11 周**：Neo4j + `query_relations`（见 `week11-graph-rag.md`） |
| 多模态（架构图/截图/幻灯） | 后期新建包，如 `data/corpus/multimodal/` | **第 12 周补**，支撑多模态 RAG |
| 开放域/易检索失败题 | 扩 golden + 可选外链语料 | **第 12 周补**，支撑 CRAG / Self-RAG |

**硬性要求**：ingest 可重跑；语料与代码解耦（换目录即可）。  
**后期分库约定**：上表每一类（及继续拆开的 HR/IT/行政等）在后期应能对应 **独立 KB + Profile + Tool**；主线第 1–6 周仍可先用统一库，第 8 周起再拆。

---

## 5. 六周详细计划

### 第 1 周：跑通最简 RAG（建立对照基线）

**目标**：有一条能问答的端到端链路，后面所有改进都和它对比。

**必做**：
- [ ] 项目骨架：`uv` · `src/` · `tests/` · `data/` · `pydantic-settings`（key 不进代码）
- [ ] 接入内部语料（MD / HTML / CSV）
- [ ] 按标题分块 → embedding → 向量库（Chroma）
- [ ] 检索 top-k → 拼 prompt → 生成答案（含重试与降级）
- [ ] 自己出题手测；**答得不对劲就记一笔**（现象即可，根因可以后补）

**做完应能讲清**：
1. 一条用户问题经过了哪些步骤？
2. 你怎么初步判断「可能是检索不行」还是「生成在瞎写」？

**产出**：可运行的 baseline；`rag-pitfalls.md` 有则记、无则空着。

---

### 第 2 周：看见内部 + 最小可靠性 + 第一套 Eval

**目标**：从「黑盒能答」变成「能看见、能降级、能量化」——这样后面撞上的问题才查得清。

**必做**：
- [ ] 接 **Langfuse**（或同等）：能看到检索 chunk、prompt、token、延迟
- [ ] LLM：超时 + 区分可重试错误 + 失败兜底
- [ ] 手写约 **15 条** golden set（来自你真实会问/已经问砸过的题）
- [ ] 最简评分 + 记下 baseline 分数

**做完应能讲清**：
1. 拿到一次坏回答，你准备按什么顺序拆：解析 / 分块 / 检索 / 生成？
2. 这 15 题里，有没有已经暴露过的失败模式？

**产出**：trace 可调试 + baseline eval。

---

### 第 3 周：★ RAG 质量工程（核心周）

**目标**：在更真实的语料与检索方案上迭代，**每改一处用同一套题对比**。

**必做（按优先级，做不完就砍后面的）**：
1. [x] （可选）补 PDF loader → **顺延第 8 周：当周补 PDF 迷你语料再练解析**（不为「现在没有」永久跳过）
2. [x] 混合检索（向量 + BM25 + 融合）——针对术语/工号等关键词场景
3. [x] 重排（cross-encoder 等）
4. [ ] Query 改写 → **明确放到第 5 周（多轮一起做），本周不提前**
5. [x] 用 eval 验证上述改动；一次只改一个变量（已有 hybrid / hybrid+rerank 结果；第 4 周补齐 vector 对照与 recall）

**可砍**：embedding 选型大战、GraphRAG。

**铁律**：改动留下 before/after。过程中的异常与回退**据实记录**。不做「先故意用错方案再纠正」。

**做完应能讲清**：
1. 当前分块/检索组合是什么？哪次改动用数字证明有效？
2. 哪些题型在加重排/混合检索后明显好转？

**产出**：改进后的检索管线 + 若干组你自己的对比数据。

---

### 第 4 周：★ Eval 体系 + 回归纪律  ← **当前应执行**

**目标**：改 RAG 能回归——否则你分不清「真的变好」还是「换了种错法」。

**必做**：
- [ ] 按你实际需求扩 golden set（建议到 30 左右；题从真实失败与边界里长出来，例如漏「双人复核」类）
- [ ] 检索指标至少 `recall@k`；生成侧继续关键词，必要时抽检或简单 judge
- [ ] （可选）`ragas` 小子集，弄懂含义即可
- [ ] 一键跑 eval 出报告：至少打出 **vector / hybrid / hybrid+rerank** 三列对照
- [ ] 若本周改动导致分数掉了：把经过写入手册（这是很值钱的真实记录）

**做完应能讲清**：
1. 你如何避免「感觉变好了」却没有依据？
2. 检索分和最终答案质量不同步时，你怎么查？

**产出**：可重复 eval + 基线记录。  
**本周不做**：工具路由、分库 Profile、HyDE、图谱（留给后期）。

---

### 第 5 周：端到端「像个产品」的 RAG

**目标**：补齐引用、拒答、多轮、最小界面——这些常在「能跑之后」才暴露产品向问题。

**必做**：
- [ ] 答案带来源/命中片段（方便你自己排查）
- [ ] 知识库未覆盖时的拒答或低置信表达（规则按你实测再定）
- [ ] 多轮 + **query 改写后再检索**（第 3 周挪过来的项）
- [ ] Gradio 或最小 API：能问、能看来源/chunk
- [ ] （可选）简单模型分级，用你的 eval 看是否掉点

**非目标（本周）**：多知识库 Agent、多 search 工具、HITL。  
（「RAG 做成工具让 LLM 选库」→ **第 9 周**。）

**产出**：本地可演示的助手。

---

### 第 6 周：复盘固化

**目标**：把**已经发生过的事**整理成可复查材料——不是补写没经历过的坑。

**必做**：
- [ ] 整理 `rag-pitfalls.md`：只保留真实条目；每条补全「怎么发现 / 根因 / 处理 / 验证」
- [ ] 系统架构图（按你真实实现画）
- [ ] README：流程 + 关键决策 + 你手头已有的数字/案例
- [ ] 短 demo：正常问答 + 一次你真实排查过的问题（对照 trace）
- [ ] 脱稿讲一遍全流程；举例只用手册里有的条目

**产出**：可运行项目 + 真实问题复盘 + 你能讲清的流程。  
完成本周后，再进入下面的 **后期计划（第 7–12 周）**。

---

## 5.1 后期计划（第 7–12 周）

> **前置条件**：第 6 周复盘完成；第 4 周的 eval / `recall@k` / 三路对照可用。  
> **后期要掌握的能力（你已明确）**：  
> **不同语料 → 不同知识库 → 不同处理策略（Profile）→ 不同工具；智能体通过选择工具完成 RAG 路由。**  
> **每周节奏**：补/拆语料为 KB → 为该 KB 配策略 →（第 9 周起）注册工具并由 Agent 调用 → 专项题验收检索质量 **与** 路由是否选对。  
> **铁律**：一次主攻一个 KB/场景；before/after；坑点事后记。缺料就补，不删项。

### 目标架构（必须按此设计）

```
                    ┌─ tool: search_hr_policy     → KB_hr      + Profile_hr
用户问题 → Agent ──┼─ tool: search_it_sop         → KB_it      + Profile_it
                    ├─ tool: search_pdf_handbook  → KB_pdf     + Profile_pdf
                    ├─ tool: query_relations      → KB_graph   + Profile_graph
                    └─ tool: search_visual        → KB_mm      + Profile_mm
                              ↓
                     各工具返回证据（chunks / 子图 / 图文描述）
                              ↓
                     Agent 综合生成（引用 / 拒答）
```

| 层 | 职责 | 不做什么 |
|----|------|----------|
| **Agent** | 选哪个工具、是否多工具、query 是否改写 | 不实现分块/BM25/重排细节 |
| **Tool** | 绑定一个 KB；执行该库 Profile 全流程 | 不越权查别的库 |
| **Profile** | 该库的解析/分块/检索/重排/过滤/纠错/prompt | 与 COMMON 合并，只覆盖差异 |
| **KB** | 一类语料的索引与元数据 | 后期不与别的语料糊成不可区分的大锅饭 |

### 场景 ↔ KB ↔ 工具 ↔ 策略（总表）

| 周 | 语料/KB | Agent 工具（示例名） | 该库差异化策略 |
|----|---------|----------------------|----------------|
| 7 | 仍可单库，先补齐通用能力 | （尚未拆工具） | 过滤、子查询、父文档/窗口 → 日后写入 COMMON 或各 Profile |
| 8 | 拆多 KB；**补 PDF KB** | 先能按 kb_id 调用；第 9 周正式挂工具 | 每库 Profile；PDF 解析；分块 A/B |
| 9 | 已有文本类多 KB | `search_hr` / `search_it` / `search_directory`… | **Agent 路由骨架**；权限/步数/空结果 |
| 10 | **生产存储改造** | （无新工具） | Qdrant + OpenSearch；物理分库；增量 ingest |
| 11 | **关系 KB** | `query_relations` | Neo4j + prose 抽取；与文档工具路由对照 |
| 12 | **多模态 KB** + 纠错 | `search_visual` + Profile 挂 CRAG | 图文检索；总复盘 |

---

### 第 7 周：检索后过滤 + 查询侧加深 + 上下文增强

**目标**：为后续「每库 Profile」准备可插拔能力；本周仍可在统一管线上开关验证。

**必做**：
- [ ] **过滤**：低相关丢掉；按 `source`/`domain` 等元数据过滤（分库预演）
- [ ] **子查询分解**：复合问题多次检索再合并
- [ ] **父文档检索或语句窗口**（二选一）
- [ ] 三项各留 before/after；并注明日后默认进 COMMON 还是某类 Profile

**产出**：可配置能力模块；对照数据入 `data/eval/results/`。

---

### 第 8 周：★ 语料分库 + Profile 分策略 + PDF KB

**目标**：落实「**一种语料 / 一个领域 = 一个 KB = 一套策略**」；为第 9 周挂工具打好注册表。

**必做**：
- [x] KB Registry：每个 KB 有 `id`、语料根目录、`profile`、（预留）`tool_name`/`description`
- [x] `COMMON_PROFILE` + 至少 **3 个分库 Profile**（例：制度文档、通讯录/CSV、PDF 手册——策略明显不同）
- [x] 物理或逻辑分库（**本项目采用逻辑分库**：统一库 + `kb` 强过滤 + 召回下推；物理分库见 `production-gap` §2.3.1 P2-5）
- [x] **补 PDF 语料包** → 独立 KB → 专属解析/分块 Profile
- [x] 分块对照（标题切 vs 固定窗口）记在对应 KB 的决策里（`kb/profiles.py` + `design-choices.md`）
- [x] 验收：给定 `kb_id` 检索时，只命中该库语料；各库题用对本库 Profile（eval 34/34，`recall@4` 31/31）

**非目标**：本周 Agent 自动选库可后置到第 9 周；本周先保证 **手动/API 指定 KB 时策略正确**。

**做完应能讲清**：
1. 为什么通讯录 KB 和制度 KB 不能共用同一套检索权重？
2. Registry 里新增一个 KB 要改哪几处？

**产出**：多 KB + 多 Profile + Registry；PDF KB 可查。

---

### 第 9 周：★ Agent RAG 路由（KB 工具化）——能力主轴周

**目标**：掌握核心能力——**智能体不直接「查大库」，而是选择知识库工具**；每个工具背后是独立 KB + Profile。

**必做**：
- [ ] 每个已注册 KB 暴露为工具（一对一优先，description 写清适用/不适用）
- [ ] Agent（function calling / 同等）根据问题选工具；允许同一轮多工具
- [ ] 硬约束：权限在工具内；最大步数；空结果信号；低置信澄清或 fallback（如 `search_all_allowed` 仅作降级）
- [ ] 观测：记录 `tool_name`、`kb_id`、query、命中数、是否选错库
- [ ] **路由专项 eval**：单库题必须选对工具；跨库题应多工具；库外题拒答或澄清
- [ ] 对照：Agent 路由 vs 强制指定正确 KB vs 错误指定 KB（证明路由层有独立价值）

**做完应能讲清**：
1. 路由错了如何拆：Agent 选错工具 vs 工具内 Profile 差 vs 生成胡写？
2. 新加一个语料类型时，如何按「KB → Profile → Tool → 路由题」扩展？

**产出**：可演示的 Agent + 多 KB 工具路由；路由评测记录。

---

### 第 10 周：★ 生产级存储改造（Qdrant + OpenSearch）

**目标**：把第 1–9 周的 RAG 骨架接到生产存储；**不换 Agent 逻辑，只换腿**。

**唯一执行文档**：[`docs/production-upgrade.md`](docs/production-upgrade.md)

**必做（按 Phase 顺序，一次只做一个）**：

| Phase | 交付 |
|-------|------|
| 0 | `docker-compose.yml`（Qdrant + OpenSearch）+ 环境变量 |
| 1 | Chroma → **Qdrant** |
| 2 | `bm25.pkl` → **OpenSearch** |
| 3 | 逻辑分库 → **物理分库** |
| 4 | 全量 ingest → **增量 upsert** |

**明确不做**：GraphRAG（顺延第 11 周，见 [`week11-graph-rag.md`](docs/week11-graph-rag.md)）。

**做完应能讲清**：
1. 向量检索与关键词检索在生产里为什么用两个存储？
2. `VECTOR_BACKEND=chroma` 与 `qdrant` 切换时，代码路径差在哪？

**产出**：`docker compose` 可跑通；`production-gap.md` 对应行标「已升级」。

---

---

### 第 11 周：★ Graph RAG（Neo4j + `query_relations`）

**目标**：关系/多跳问题走图检索；**必须**走生产路径（prose → 抽取 → Neo4j → Cypher），不做 JSON 内存图。

**唯一执行文档**：[`docs/week11-graph-rag.md`](docs/week11-graph-rag.md)

**必做**：
- [ ] prose 语料 + `ingest-graph` → Neo4j
- [ ] `query_relations` + ReAct 第四工具
- [ ] 路由 eval：关系题 vs 制度题
- [ ] before/after 关系题

**产出**：图工具挂 Agent；能讲清「文档 RAG vs 图 RAG」分工。

---

### 第 12 周：★ 多模态 + CRAG + 总复盘

**必做**：
- [ ] 补图/截图/幻灯语料 → `KB_multimodal`；工具 `search_visual`（精简版可接受）
- [ ] 补易失败专项题；**CRAG / Self-RAG** 挂 Profile（精简版）
- [ ] 总复盘：全部 KB × Profile × 工具 × 路由结果
- [ ] 脱稿讲清扩展法：「KB → 策略 → 工具 → Agent」

**产出**：多模态 + 纠错可演示；第 7–12 周能力复盘完成。

**仍可后置**：嵌入微调、公网部署、求职包装。

---

## 6. 每周自检（周日 20 分钟）

```
□ 本周链路哪一环变了？
□ 有没有 before/after（数字或具体问答案例）？
□ 本周是否遇到异常/失败/回退？有 → 是否已写入 rag-pitfalls.md？
□ 对照 production-gap §1：能指出本周一处 demo 简化 + 生产替代方案吗？
□ 下周是否只开一条线？
□ 关掉 AI 助手，我还能讲清本周决策吗？
```

**红线**：连续两周没有可运行进展 → 停课回到主线。  
**不是红线**：某周坑点手册没有新增。

---

## 7. 进度锚点

| 节点 | Definition of Done |
|------|-------------------|
| 第 2 周末 | baseline RAG + Langfuse + 第一套 eval 基线 ✅ |
| 第 3 周末 | hybrid + rerank 落地，有 eval 记录 ✅（改写顺延第 5 周） |
| 第 4 周末 | 扩题 + `recall@k` + vector/hybrid/rerank 一键对照 |
| 第 6 周末 | 可演示；流程能脱稿讲；手册 = 过程实录（不凑数） |
| 第 8 周末 | 多 KB + 多 Profile + Registry；PDF KB 可按 kb_id 正确检索 |
| 第 9 周末 | Agent 经工具选 KB；路由专项 eval 通过（选对工具） |
| 第 10 周末 | Qdrant + OpenSearch；物理分库 + 增量 ingest；eval 通过 ✅ 2026-08-16：recall 31/31，routing 6/6，答案 33/34 |
| 第 11 周末 | Neo4j 关系 KB + `query_relations`；路由对照 |
| 第 12 周末 | 多模态 + CRAG 精简版；12 周总复盘 |

---

## 8. 技术栈（够用即可，禁止追新）

- **骨架**：`uv` · `pydantic-settings` · 结构化日志 · `tenacity`
- **解析**：Markdown · `pypdf`/`unstructured` · `trafilatura`
- **检索**：Chroma 或 Qdrant · `rank_bm25` · reranker（需要时再上）
- **编排**：LangChain 或 LangGraph，选一个不中途换
- **观测**：Langfuse
- **Eval**：自建 golden set 为主
- **界面**：Gradio 或最小 FastAPI

---

## 9. 资源（按需查阅）

- Anthropic《Contextual Retrieval》
- Eugene Yan《Evaluating LLM Applications》
- LangChain / LangGraph RAG cookbook
- Langfuse 文档

遇到具体报错/坏案例再查，不要先刷「RAG 十大坑」再开工。

---

## 10. 风险与取舍

1. **为目标而找坑**：停。继续做下一周能力即可。
2. **抄清单进手册**：手册作废，只留你自己的条目。
3. **目标漂移**：第 1–6 周禁止提前做工具路由 / GraphRAG / 多模态；这些在第 7–12 周按场景练。
4. **框架 hopping**：禁止。
5. **没有依据的优化**：尽量避免；至少留问答案例。
6. **产品一条线，场景可扩展**：星云助手为载体；后期按周**补语料扩场景**，不是另起毫无关联的玩具项目。
7. **不为缺料跳过策略**：到周次发现缺语料 → 当周先补迷你包再练；「现在没有」不是删项理由。
8. **场景周允许砍深度、不允许砍场景**：例如 GraphRAG 用可跑通精简版，但第 10 周场景本身不取消。

---

## 11. 当前立刻做的事（按进度）

1. **当前进度**：以代码与 `pytest` / eval 为准；文档以 [`production-gap.md`](docs/production-gap.md) 为 demo vs 生产准绳  
2. **不要并行**：一次只开一条改造线（例如先 Qdrant，再 OpenSearch）  
3. 遇选型问题先查 `production-gap`，不要等踩坑才补文档

---

## 附录 A：`rag-pitfalls.md` 条目模板（事后用）

撞上再填，没有就整份文件可以几乎是空的：

```markdown
### （一句话描述你当时遇到的问题）
- 时间/周次：
- 当时在做：
- 现象：
- 如何发现：手测 / trace / eval / 报错 …
- 根因：（查清后再写，当时不明就写「待查」）
- 处理：
- 验证：改完后怎样
```

## 附录 B：六周结束时能讲清即可（结合你的实录举例）

1. 从文件到答案，你的数据流是什么？
2. 分块/检索上你做过哪些对比？依据是什么？
3. 一次坏答案你怎么拆到具体环节？
4. 语料或索引更新时你怎么处理的？
5. 手册里挑 2–3 条真实问题，讲清楚来龙去脉

（若某条你过程中没碰到，就说「我这套实现里还没遇到」，诚实即可。）

---

## 附录 C：第 12 周以后仍可选

- 部署 + 真实用户反馈
- 求职叙事（素材只来自实录与数字）
- 嵌入微调（需更大规模评测再开）
- 某一场景的生产级加深（在已有对照基础上单点加厚）

## 附录 D：后期架构目标（能力主轴）

```
用户问题
  → Agent（function calling）
  → 每个知识库一个工具（语料不同 → 策略不同 → 工具不同）
  → Tool 内：该 KB 的 Profile（解析/分块/检索/重排/过滤/可选 CRAG·Self-RAG）
  → 返回证据
  → Agent 综合生成（引用 + 拒答）
```

**扩展新语料的标准动作**（第 10/11 周反复练）：  
`补语料包 → 注册 KB → 写差异 Profile → 挂 Tool + description → 加路由专项题 → before/after`

权限、最大步数、空结果、fallback 在工具/编排层做死。
